<Window x:Class="applanch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:applanch.Controls"
        xmlns:local="clr-namespace:applanch"
        xmlns:vm="clr-namespace:applanch.ViewModels"
        mc:Ignorable="d"
        xmlns:res="clr-namespace:applanch.Properties"
        Title="Applanch"
        Height="560"
        Width="840"
        MinHeight="480"
        MinWidth="700"
        AllowDrop="True"
        SourceInitialized="Window_SourceInitialized"
        Loaded="MainWindow_Loaded"
        DragOver="Window_DragOver"
        Drop="Window_Drop"
        Background="{DynamicResource Brush.AppBackground}">
    <Window.Resources>
        <Style x:Key="CategorySidebarHoverZoneStyle"
               TargetType="Border">
            <Setter Property="Visibility"
                Value="Visible"/>
            <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=CategorySidebarPinToggleButton}"
                     Value="True">
                <Setter Property="Visibility"
                    Value="Collapsed"/>
            </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategorySidebarContainerStyle"
               TargetType="Border">
            <Setter Property="Padding"
                Value="0"/>
            <Style.Triggers>
            <DataTrigger Binding="{Binding IsCategorySidebarExpanded, RelativeSource={RelativeSource AncestorType=Window}}"
                     Value="True">
                <Setter Property="Padding"
                    Value="12"/>
            </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategorySidebarExpandedContentStyle"
               TargetType="Grid">
            <Setter Property="Visibility"
                Value="Collapsed"/>
            <Setter Property="IsHitTestVisible"
                Value="False"/>
            <Style.Triggers>
            <DataTrigger Binding="{Binding IsCategorySidebarExpanded, RelativeSource={RelativeSource AncestorType=Window}}"
                     Value="True">
                <Setter Property="Visibility"
                    Value="Visible"/>
                <Setter Property="IsHitTestVisible"
                    Value="True"/>
            </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MainContentContainerStyle"
               TargetType="Grid">
            <Setter Property="Margin"
                    Value="188,0,0,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsChecked, ElementName=CategorySidebarPinToggleButton}"
                             Value="False">
                    <Setter Property="Margin"
                            Value="0"/>
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                    To="0"
                                                    Duration="0:0:0.22">
                                    <ThicknessAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseInOut"/>
                                    </ThicknessAnimation.EasingFunction>
                                </ThicknessAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                    To="188,0,0,0"
                                                    Duration="0:0:0.22">
                                    <ThicknessAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseInOut"/>
                                    </ThicknessAnimation.EasingFunction>
                                </ThicknessAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background"
                    Value="{DynamicResource Brush.Surface}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource Brush.ItemBorder}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Padding"
                    Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="16"
                                                  ShadowDepth="4"
                                                  Opacity="0.18"
                                                  Color="#000000"/>
                            </Border.Effect>
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground"
                    Value="{DynamicResource Brush.TextPrimary}"/>
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Padding"
                    Value="14,8"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Margin="2,1">
                            <ContentPresenter Content="{TemplateBinding Header}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted"
                                     Value="True">
                                <Setter TargetName="MenuItemBorder"
                                        Property="Background"
                                        Value="{DynamicResource Brush.ItemBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter Property="Opacity"
                                        Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}"
               TargetType="Separator">
            <Setter Property="Margin"
                    Value="4,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1"
                                Background="{DynamicResource Brush.ItemBorder}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator"
               BasedOn="{StaticResource {x:Static MenuItem.SeparatorStyleKey}}"/>

        <Style x:Key="RoundedSecondaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource RoundedButtonStyle}"/>

        <Style x:Key="HeaderIconButtonStyle"
               TargetType="Button">
            <Setter Property="Width"
                    Value="32"/>
            <Setter Property="Height"
                    Value="32"/>
            <Setter Property="Padding"
                    Value="0"/>
            <Setter Property="VerticalAlignment"
                    Value="Top"/>
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderBrush"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource Brush.TextSecondary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="Transparent"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Foreground"
                                        Value="{DynamicResource Brush.TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderToggleCheckBoxStyle"
               TargetType="CheckBox">
            <Setter Property="Foreground"
                Value="{DynamicResource Brush.TextPrimary}"/>
            <Setter Property="FontSize"
                Value="12"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Track"
                                    Grid.Column="2"
                                    Width="38"
                                    Height="22"
                                    Background="{DynamicResource Brush.Surface}"
                                    BorderBrush="{DynamicResource Brush.ItemBorder}"
                                    BorderThickness="1"
                                    CornerRadius="11">
                                <Ellipse x:Name="Thumb"
                                         Width="16"
                                         Height="16"
                                         Margin="2"
                                         HorizontalAlignment="Left"
                                         Fill="{DynamicResource Brush.TextTertiary}"/>
                            </Border>
                            <ContentPresenter Grid.Column="0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                     Value="True">
                                <Setter TargetName="Track"
                                        Property="Background"
                                    Value="#1A6DB3F8"/>
                                <Setter TargetName="Track"
                                        Property="BorderBrush"
                                    Value="#6DB3F8"/>
                                <Setter TargetName="Thumb"
                                        Property="HorizontalAlignment"
                                        Value="Right"/>
                                <Setter TargetName="Thumb"
                                        Property="Fill"
                                    Value="#6DB3F8"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Track"
                                        Property="BorderBrush"
                                    Value="#8DBFFB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter Property="Opacity"
                                        Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LaunchTileButtonStyle"
               TargetType="Button">
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Padding"
                    Value="8"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="TileBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="TileBorder"
                                        Property="Background"
                                        Value="{DynamicResource Brush.Surface}"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="TileBorder"
                                        Property="Opacity"
                                        Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxItemStyle"
               TargetType="ComboBoxItem">
            <Setter Property="Foreground"
                    Value="{DynamicResource Brush.TextPrimary}"/>
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Padding"
                    Value="10,7"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted"
                                     Value="True">
                                <Setter TargetName="ItemBorder"
                                        Property="Background"
                                        Value="{DynamicResource Brush.ItemBackground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter TargetName="ItemBorder"
                                        Property="Background"
                                        Value="{DynamicResource Brush.ItemBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Implicit style makes this the default for ALL ComboBoxItems,
                     overriding the WPF theme style that has FindAncestor bindings. -->
        <Style TargetType="ComboBoxItem"
               BasedOn="{StaticResource ModernComboBoxItemStyle}"/>

        <Style x:Key="ModernComboBoxToggleButtonStyle"
               TargetType="ToggleButton">
            <Setter Property="Focusable"
                    Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent"
                                BorderThickness="0">
                            <Path Width="10"
                                  Height="6"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 5 6 L 10 0"
                                  Stroke="{DynamicResource Brush.TextTertiary}"
                                  StrokeThickness="1.6"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxStyle"
               TargetType="ComboBox">
            <Setter Property="Foreground"
                    Value="{DynamicResource Brush.TextPrimary}"/>
            <Setter Property="Background"
                    Value="{DynamicResource Brush.Surface}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource Brush.ItemBorder}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Padding"
                    Value="10,6"/>
            <Setter Property="MinHeight"
                    Value="34"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Stretch"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
                    Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
                    Value="Auto"/>
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource ModernComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="OuterBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="32"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter x:Name="ContentSite"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      IsHitTestVisible="False"/>

                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Margin="{TemplateBinding Padding}"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Stretch"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{TemplateBinding Foreground}"
                                             CaretBrush="{DynamicResource Brush.TextPrimary}"
                                             Visibility="Hidden"/>

                                    <ToggleButton Grid.Column="1"
                                                  ClickMode="Press"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  Style="{StaticResource ModernComboBoxToggleButtonStyle}"/>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border Margin="0,6,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="240"
                                        Background="{DynamicResource Brush.Surface}"
                                        BorderBrush="{DynamicResource Brush.ItemBorder}"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <ScrollViewer Margin="4"
                                                  SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="OuterBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource Brush.SurfaceBorder}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin"
                                     Value="True">
                                <Setter TargetName="OuterBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource Brush.SurfaceBorder}"/>
                                <Setter TargetName="OuterBorder"
                                        Property="Background"
                                        Value="{DynamicResource Brush.ItemBackground}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen"
                                     Value="True">
                                <Setter TargetName="OuterBorder"
                                        Property="BorderBrush"
                                        Value="{DynamicResource Brush.SurfaceBorder}"/>
                                <Setter TargetName="OuterBorder"
                                        Property="Background"
                                        Value="{DynamicResource Brush.ItemBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEditable"
                                     Value="True">
                                <Setter TargetName="PART_EditableTextBox"
                                        Property="Visibility"
                                        Value="Visible"/>
                                <Setter TargetName="ContentSite"
                                        Property="Visibility"
                                        Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter TargetName="OuterBorder"
                                        Property="Opacity"
                                        Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="QuickAddCompactComboBoxStyle"
               TargetType="ComboBox"
               BasedOn="{StaticResource ModernComboBoxStyle}">
            <Setter Property="MinHeight"
                    Value="28"/>
            <Setter Property="Padding"
                    Value="10,3"/>
        </Style>

        <Style x:Key="ModernTextBoxStyle"
               TargetType="TextBox"
               BasedOn="{StaticResource RoundedTextBoxStyle}"/>

        <Style x:Key="QuickAddCompactTextBoxStyle"
               TargetType="TextBox"
               BasedOn="{StaticResource ModernTextBoxStyle}">
            <Setter Property="MinHeight"
                    Value="28"/>
            <Setter Property="Padding"
                    Value="10,4"/>
        </Style>

        <Style x:Key="ModernScrollBarThumbStyle"
               TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels"
                    Value="True"/>
            <Setter Property="OverridesDefaultStyle"
                    Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                CornerRadius="3"
                                Background="{DynamicResource Brush.TextTertiary}"
                                Opacity="0.45"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="ThumbBorder"
                                        Property="Opacity"
                                        Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsDragging"
                                     Value="True">
                                <Setter TargetName="ThumbBorder"
                                        Property="Opacity"
                                        Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBarPageButtonStyle"
               TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels"
                    Value="True"/>
            <Setter Property="OverridesDefaultStyle"
                    Value="True"/>
            <Setter Property="Focusable"
                    Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBarTemplate"
                         TargetType="ScrollBar">
            <Grid Width="6"
                  Margin="0,2">
                <Track x:Name="PART_Track"
                       IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ModernScrollBarPageButtonStyle}"
                                      Command="ScrollBar.PageUpCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ModernScrollBarPageButtonStyle}"
                                      Command="ScrollBar.PageDownCommand"/>
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ModernScrollBarThumbStyle}"
                               Margin="1,0"/>
                    </Track.Thumb>
                </Track>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="HorizontalScrollBarTemplate"
                         TargetType="ScrollBar">
            <Grid Height="6"
                  Margin="2,0">
                <Track x:Name="PART_Track">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ModernScrollBarPageButtonStyle}"
                                      Command="ScrollBar.PageLeftCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ModernScrollBarPageButtonStyle}"
                                      Command="ScrollBar.PageRightCommand"/>
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ModernScrollBarThumbStyle}"
                               Margin="0,1"/>
                    </Track.Thumb>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels"
                    Value="True"/>
            <Setter Property="OverridesDefaultStyle"
                    Value="True"/>
            <Style.Triggers>
                <Trigger Property="Orientation"
                         Value="Vertical">
                    <Setter Property="Width"
                            Value="6"/>
                    <Setter Property="MinWidth"
                            Value="6"/>
                    <Setter Property="Template"
                            Value="{StaticResource VerticalScrollBarTemplate}"/>
                </Trigger>
                <Trigger Property="Orientation"
                         Value="Horizontal">
                    <Setter Property="Height"
                            Value="6"/>
                    <Setter Property="MinHeight"
                            Value="6"/>
                    <Setter Property="Template"
                            Value="{StaticResource HorizontalScrollBarTemplate}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:FloatingNotificationControl x:Name="FloatingNotification"
                                              Grid.Row="0"
                                              Grid.RowSpan="4"
                                              Panel.ZIndex="20"
                                              Message="{Binding FloatingNotification.Message}"
                                              IconType="{Binding FloatingNotification.IconType}"
                                              ActionText="{Binding FloatingNotification.ActionText}"
                                              ActionVisibility="{Binding FloatingNotification.ActionVisibility}"
                                              ActionRequested="FloatingNotification_ActionRequested"
                                              Hidden="FloatingNotification_Hidden"/>

        <controls:HeaderBarControl Grid.Row="0"
                                   UpdateButtonVisibility="{Binding UpdateBanner.HeaderButtonVisibility}"
                                   UpdateRequested="UpdateButton_Click"
                                   SettingsRequested="SettingsButton_Click"/>

        <controls:UpdateBannerControl Grid.Row="1"
                                      Margin="0,8,0,0"
                                      Message="{Binding UpdateBanner.Message}"
                                      Visibility="{Binding UpdateBanner.BannerVisibility}"
                                      UpdateActionButtonVisibility="{Binding UpdateBanner.ActionButtonVisibility}"
                                      UpdateRequested="UpdateButton_Click"
                                      DismissRequested="DismissUpdateButton_Click"/>

        <Grid Grid.Row="3">
            <Border x:Name="CategorySidebarHoverZone"
                    Style="{StaticResource CategorySidebarHoverZoneStyle}"
                Width="12"
                    Margin="-12,0,0,0"
                    HorizontalAlignment="Left"
                    Background="Transparent"
                    Panel.ZIndex="5"
                    MouseEnter="CategorySidebarHoverZone_MouseEnter"
                    MouseLeave="CategorySidebarHoverZone_MouseLeave"/>

            <Border x:Name="CategorySidebarContainer"
                    Style="{StaticResource CategorySidebarContainerStyle}"
                Width="172"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Stretch"
                    Background="{DynamicResource Brush.Surface}"
                    BorderBrush="{DynamicResource Brush.ItemBorder}"
                    BorderThickness="1"
                    CornerRadius="8"
                    ClipToBounds="True"
                    Panel.ZIndex="10"
                    MouseEnter="CategorySidebarContainer_MouseEnter"
                    MouseLeave="CategorySidebarContainer_MouseLeave">
                <Grid>
                    <Grid x:Name="CategorySidebarExpandedContent"
                          Style="{StaticResource CategorySidebarExpandedContentStyle}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[SectionLabel_Category]}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.TextPrimary}"
                                       VerticalAlignment="Center"/>

                            <ToggleButton x:Name="CategorySidebarPinToggleButton"
                                          Grid.Column="1"
                                          Width="24"
                                          Height="24"
                                          Margin="8,0,0,0"
                                          Padding="0"
                                          Background="Transparent"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          Cursor="Hand"
                                          Foreground="{DynamicResource Brush.TextPrimary}"
                                          FontSize="12"
                                          IsChecked="True"
                                          Checked="CategorySidebarPinToggleButton_Checked"
                                          Unchecked="CategorySidebarPinToggleButton_Unchecked">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="ToolTip"
                                                Value="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_UnpinCategorySidebar]}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="ToggleBorder"
                                                            Background="Transparent"
                                                            CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver"
                                                                 Value="True">
                                                            <Setter TargetName="ToggleBorder"
                                                                    Property="Background"
                                                                    Value="{DynamicResource Brush.ItemBackground}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsChecked"
                                                                 Value="False">
                                                            <Setter Property="Opacity"
                                                                    Value="0.55"/>
                                                            <Setter Property="ToolTip"
                                                                    Value="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_PinCategorySidebar]}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                                <Grid Width="14"
                                      Height="14">
                                    <TextBlock Text="📌"
                                               FontSize="12"
                                               FontWeight="Normal"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <Border Width="14"
                                            Height="1.4"
                                            Background="{DynamicResource Brush.TextPrimary}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Opacity="0.9"
                                            RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <RotateTransform Angle="-35"/>
                                        </Border.RenderTransform>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                                                 Value="False">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </Grid>
                            </ToggleButton>
                        </Grid>

                        <ListBox Grid.Row="2"
                                 ItemsSource="{Binding FilterCategoryNames}"
                                 SelectedItem="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0"
                                 Background="Transparent">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding"
                                            Value="0"/>
                                    <Setter Property="Margin"
                                            Value="0,0,0,4"/>
                                    <Setter Property="HorizontalContentAlignment"
                                            Value="Stretch"/>
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource Brush.TextSecondary}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="ItemBorder"
                                                        Padding="10,6"
                                                        CornerRadius="6"
                                                        Background="Transparent"
                                                        BorderThickness="1"
                                                        BorderBrush="Transparent">
                                                    <ContentPresenter HorizontalAlignment="Stretch"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver"
                                                             Value="True">
                                                        <Setter Property="Foreground"
                                                                Value="{DynamicResource Brush.TextPrimary}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected"
                                                             Value="True">
                                                        <Setter TargetName="ItemBorder"
                                                                Property="Background"
                                                                Value="{DynamicResource Brush.ItemBackground}"/>
                                                        <Setter TargetName="ItemBorder"
                                                                Property="BorderBrush"
                                                                Value="{DynamicResource Brush.SurfaceBorder}"/>
                                                        <Setter Property="Foreground"
                                                                Value="{DynamicResource Brush.TextPrimary}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               FontSize="13"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Grid>
            </Border>

            <Grid x:Name="MainContentContainer"
                Style="{StaticResource MainContentContainerStyle}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Expander Grid.Column="0"
                              Grid.ColumnSpan="2"
                              Foreground="{DynamicResource Brush.TextPrimary}"
                              HorizontalContentAlignment="Stretch">
                        <Expander.Header>
                            <TextBlock Text="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Expander_AddApp]}"
                                       VerticalAlignment="Center"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.TextPrimary}"/>
                        </Expander.Header>
                        <Border Margin="0,8,0,0"
                                Padding="12"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource Brush.ItemBorder}"
                                Background="{DynamicResource Brush.Surface}"
                                CornerRadius="8">
                            <StackPanel>
                            <controls:SuggestionInputControl
                                InputComboBoxStyle="{StaticResource ModernComboBoxStyle}"
                                Suggestions="{Binding QuickAddSuggestions}"
                                Text="{Binding QuickAddNameOrPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_NameOrPath]}"/>
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="86"/>
                                </Grid.ColumnDefinitions>
                                <controls:SuggestionInputControl Grid.Column="0"
                                                                 InputComboBoxStyle="{StaticResource QuickAddCompactComboBoxStyle}"
                                                                 Suggestions="{Binding CategoryNames}"
                                                                 Text="{Binding QuickAddCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_Category]}"/>
                                <TextBox Grid.Column="2"
                                         Text="{Binding QuickAddArguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource QuickAddCompactTextBoxStyle}"
                                         ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_Arguments]}"/>
                                <Button Grid.Column="4"
                                        Content="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Button_Add]}"
                                        Click="QuickAddButton_Click"
                                        Height="28"
                                        Style="{StaticResource RoundedSecondaryButtonStyle}"/>
                            </Grid>
                            <TextBlock Text="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Hint_AddApp]}"
                                       Margin="0,8,0,0"
                                       FontSize="11"
                                       Foreground="{DynamicResource Brush.TextTertiary}"/>
                            <TextBlock Margin="0,6,0,0"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Text="{Binding QuickAddFeedback.Message}"
                                       Visibility="{Binding QuickAddFeedback.MessageVisibility}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground"
                                                Value="{DynamicResource Brush.QuickAddInfoText}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding QuickAddFeedback.Severity}"
                                                         Value="{x:Static vm:QuickAddMessageSeverity.Warning}">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource Brush.QuickAddWarningText}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            </StackPanel>
                        </Border>
                    </Expander>
                    <CheckBox Grid.Column="1"
                              Margin="12,0,0,0"
                              VerticalAlignment="Top"
                              Content="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Button_IconMode]}"
                              Style="{StaticResource HeaderToggleCheckBoxStyle}"
                              IsChecked="{Binding IsLaunchItemIconOnlyMode, Mode=OneWay}"
                              Click="IconModeToggleButton_Click"
                              ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_ToggleLaunchItemIconOnlyMode]}"/>
                </Grid>

                <Grid Grid.Row="3">
                    <ListBox x:Name="LaunchListBox"
                             ItemsSource="{Binding FilteredLaunchItems}"
                             SelectedItem="{Binding SelectedLaunchItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             AllowDrop="True"
                             PreviewMouseLeftButtonDown="LaunchListBox_PreviewMouseLeftButtonDown"
                             PreviewMouseMove="LaunchListBox_PreviewMouseMove"
                             PreviewMouseWheel="LaunchListBox_PreviewMouseWheel"
                             DragOver="LaunchListBox_DragOver"
                             Drop="LaunchListBox_Drop"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.Style>
                            <Style TargetType="ListBox">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLaunchItemIconOnlyMode}"
                                                 Value="True">
                                        <Setter Property="HorizontalContentAlignment"
                                                Value="Left"/>
                                        <Setter Property="ItemsPanel">
                                            <Setter.Value>
                                                <ItemsPanelTemplate>
                                                    <controls:VirtualizingWrapPanel ItemWidth="82"/>
                                                </ItemsPanelTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.Style>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding"
                                        Value="0"/>
                                <Setter Property="Margin"
                                        Value="0,0,0,6"/>
                                <Setter Property="BorderThickness"
                                        Value="0"/>
                                <Setter Property="Background"
                                        Value="Transparent"/>
                                <Setter Property="FocusVisualStyle"
                                        Value="{x:Null}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="ItemContainerBorder"
                                                    Margin="1"
                                                    Padding="0"
                                                    CornerRadius="8"
                                                    Background="Transparent"
                                                    BorderBrush="Transparent"
                                                    BorderThickness="1">
                                                <ContentPresenter HorizontalAlignment="Stretch"
                                                                  VerticalAlignment="Stretch"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver"
                                                         Value="True">
                                                    <Setter TargetName="ItemContainerBorder"
                                                            Property="BorderBrush"
                                                            Value="{DynamicResource Brush.ItemBorder}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected"
                                                         Value="True">
                                                    <Setter TargetName="ItemContainerBorder"
                                                            Property="BorderBrush"
                                                            Value="{DynamicResource Brush.SurfaceBorder}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.IsLaunchItemIconOnlyMode, RelativeSource={RelativeSource AncestorType=Window}}"
                                                 Value="True">
                                        <Setter Property="Margin"
                                                Value="0,0,8,8"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource Brush.ItemBorder}"
                                        BorderThickness="1"
                                        Background="{DynamicResource Brush.ItemBackground}"
                                        CornerRadius="8"
                                        Padding="8,6"
                                        SnapsToDevicePixels="True">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.IsLaunchItemIconOnlyMode, RelativeSource={RelativeSource AncestorType=Window}}"
                                                             Value="True">
                                                    <Setter Property="Width"
                                                            Value="72"/>
                                                    <Setter Property="Padding"
                                                            Value="10"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Menu_Rename]}"
                                                      Tag="{x:Static local:LaunchItemContextMenuAction.Rename}"
                                                      Click="ContextMenu_Item_Click"/>
                                            <MenuItem Header="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Menu_ChangeCategory]}"
                                                      Tag="{x:Static local:LaunchItemContextMenuAction.EditCategory}"
                                                      Click="ContextMenu_Item_Click"/>
                                            <MenuItem Header="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Menu_ChangeArguments]}"
                                                      Tag="{x:Static local:LaunchItemContextMenuAction.EditArguments}"
                                                      Click="ContextMenu_Item_Click"/>
                                            <MenuItem Header="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Menu_OpenFileLocation]}"
                                                      Tag="{x:Static local:LaunchItemContextMenuAction.OpenLocation}"
                                                      Click="ContextMenu_Item_Click"/>
                                            <Separator/>
                                            <MenuItem Header="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Menu_Delete]}"
                                                      Tag="{x:Static local:LaunchItemContextMenuAction.Delete}"
                                                      Click="ContextMenu_Item_Click"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <Grid>
                                    <Grid>
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Visibility"
                                                        Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DataContext.IsLaunchItemIconOnlyMode, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                 Value="True">
                                                        <Setter Property="Visibility"
                                                                Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="26"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="::"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"
                                                   FontSize="11"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource Brush.TextTertiary}"
                                                   ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_DragToReorder]}"/>

                                        <Button Grid.Column="2"
                                                Click="LaunchItemButton_Click"
                                                Tag="{Binding}"
                                                ToolTip="{Binding DisplayName}">
                                            <Button.Style>
                                                <Style TargetType="Button"
                                                       BasedOn="{StaticResource LaunchTileButtonStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRenaming}"
                                                                     Value="True">
                                                            <Setter Property="Visibility"
                                                                    Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="30"/>
                                                    <ColumnDefinition Width="10"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.RowSpan="2"
                                                        Grid.Column="0"
                                                        Width="28"
                                                        Height="28"
                                                        CornerRadius="5"
                                                        Background="{DynamicResource Brush.IconBackground}"
                                                        VerticalAlignment="Center">
                                                    <Grid>
                                                        <Image Source="{Binding IconSource}"
                                                               Width="16"
                                                               Height="16"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                        <TextBlock Text="&#xE7BA;"
                                                                   FontFamily="Segoe MDL2 Assets"
                                                                   FontSize="10"
                                                                   HorizontalAlignment="Right"
                                                                   VerticalAlignment="Bottom"
                                                                   Margin="0,0,-1,-1"
                                                                   Foreground="{DynamicResource Brush.NotificationWarningBorder}"
                                                                   ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_MissingPath]}">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility"
                                                                            Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsPathMissing}"
                                                                                     Value="True">
                                                                            <Setter Property="Visibility"
                                                                                    Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Grid>
                                                </Border>

                                                <TextBlock Grid.Column="2"
                                                           Text="{Binding DisplayName}"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource Brush.TextPrimary}"/>

                                                <TextBlock Grid.Row="1"
                                                           Grid.Column="2"
                                                           Margin="0,2,0,0"
                                                           Text="{Binding FullPath}"
                                                           FontSize="11"
                                                           Foreground="{DynamicResource Brush.TextTertiary}"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </Grid>
                                        </Button>

                                        <Grid Grid.Column="2"
                                              Margin="8">
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Setter Property="Visibility"
                                                            Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRenaming}"
                                                                     Value="True">
                                                            <Setter Property="Visibility"
                                                                    Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.RowSpan="2"
                                                    Grid.Column="0"
                                                    Width="28"
                                                    Height="28"
                                                    CornerRadius="5"
                                                    Background="{DynamicResource Brush.IconBackground}"
                                                    VerticalAlignment="Center">
                                                <Grid>
                                                    <Image Source="{Binding IconSource}"
                                                           Width="16"
                                                           Height="16"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                                    <TextBlock Text="&#xE7BA;"
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="10"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Bottom"
                                                               Margin="0,0,-1,-1"
                                                               Foreground="{DynamicResource Brush.NotificationWarningBorder}"
                                                               ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_MissingPath]}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility"
                                                                        Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsPathMissing}"
                                                                                 Value="True">
                                                                        <Setter Property="Visibility"
                                                                                Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>

                                            <TextBox Grid.Column="2"
                                                     Text="{Binding EditingName, UpdateSourceTrigger=PropertyChanged}"
                                                     FontSize="13"
                                                     FontWeight="SemiBold"
                                                     Foreground="{DynamicResource Brush.TextPrimary}"
                                                     Background="Transparent"
                                                     BorderThickness="0"
                                                     Padding="0"
                                                     VerticalAlignment="Center"
                                                     IsVisibleChanged="RenameTextBox_IsVisibleChanged"
                                                     KeyDown="RenameTextBox_KeyDown"
                                                     LostFocus="RenameTextBox_LostFocus"/>

                                            <TextBlock Grid.Row="1"
                                                       Grid.Column="2"
                                                       Margin="0,2,0,0"
                                                       Text="{Binding FullPath}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource Brush.TextTertiary}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </Grid>

                                        <Button Grid.Column="3"
                                                Content="×"
                                                Click="DeleteButton_Click"
                                                Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button"
                                                       BasedOn="{StaticResource HeaderIconButtonStyle}">
                                                    <Setter Property="Width"
                                                            Value="26"/>
                                                    <Setter Property="Height"
                                                            Value="26"/>
                                                    <Setter Property="FontSize"
                                                            Value="13"/>
                                                    <Setter Property="FontWeight"
                                                            Value="SemiBold"/>
                                                    <Setter Property="VerticalAlignment"
                                                            Value="Center"/>
                                                    <Setter Property="ToolTip"
                                                            Value="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_Delete]}"/>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>

                                    <Button Click="LaunchItemButton_Click"
                                            Tag="{Binding}"
                                            Width="60"
                                            Height="60"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            ToolTip="{Binding DisplayName}">
                                        <Button.Style>
                                            <Style TargetType="Button"
                                                   BasedOn="{StaticResource LaunchTileButtonStyle}">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Setter Property="Padding"
                                                        Value="6"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DataContext.IsLaunchItemIconOnlyMode, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                 Value="True">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                        <Grid>
                                            <Border Width="44"
                                                    Height="44"
                                                    CornerRadius="9"
                                                    Background="{DynamicResource Brush.IconBackground}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center">
                                                <Grid>
                                                    <Image Source="{Binding IconSource}"
                                                           Width="28"
                                                           Height="28"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                                    <TextBlock Text="&#xE7BA;"
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="12"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Bottom"
                                                               Margin="0,0,0,0"
                                                               Foreground="{DynamicResource Brush.NotificationWarningBorder}"
                                                               ToolTip="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[ToolTip_MissingPath]}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility"
                                                                        Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsPathMissing}"
                                                                                 Value="True">
                                                                        <Setter Property="Visibility"
                                                                                Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Border Padding="18"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            CornerRadius="8"
                            Background="{DynamicResource Brush.Surface}"
                            BorderBrush="{DynamicResource Brush.ItemBorder}"
                            BorderThickness="1"
                            Visibility="{Binding EmptyMessageVisibility}">
                        <TextBlock Text="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[EmptyMessage]}"
                                   FontSize="13"
                                   Foreground="{DynamicResource Brush.TextTertiary}"
                                   TextAlignment="Center"/>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
