<UserControl x:Class="applanch.Controls.UpdateBannerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:applanch.Properties"
             xmlns:local="clr-namespace:applanch"
             x:Name="Root">
            <UserControl.Resources>
            <Style x:Key="BannerFlatActionButtonStyle"
                   TargetType="Button">
                <Setter Property="Padding"
                    Value="10,4"/>
                <Setter Property="MinWidth"
                    Value="72"/>
                <Setter Property="Height"
                    Value="28"/>
                <Setter Property="Background"
                    Value="Transparent"/>
                <Setter Property="BorderBrush"
                    Value="Transparent"/>
                <Setter Property="BorderThickness"
                    Value="0"/>
                <Setter Property="Cursor"
                    Value="Hand"/>
                <Setter Property="Foreground"
                    Value="{DynamicResource Brush.TextSecondary}"/>
                <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                             Value="True">
                        <Setter Property="Foreground"
                            Value="{DynamicResource Brush.TextPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
                </Setter>
            </Style>
            </UserControl.Resources>
    <Border Background="#1A6DB3F8"
            BorderBrush="#6DB3F8"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource Brush.TextPrimary}"
                       Text="{Binding Message, ElementName=Root}"/>
            <Button Grid.Column="1"
                    x:Name="UpdateActionButton"
                    Content="{Binding Source={x:Static local:LocalizedStrings.Instance}, Path=[Banner_UpdateButton]}"
                    Style="{StaticResource BannerFlatActionButtonStyle}"
                    Click="UpdateButton_Click"
                    Height="28"/>
            <Button Grid.Column="3"
                    Content="✕"
                    Click="DismissButton_Click"
                    Width="26"
                    Height="26"
                    Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Foreground="{DynamicResource Brush.TextSecondary}"
                    FontSize="13"
                    FontWeight="SemiBold"
                    VerticalAlignment="Center">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
