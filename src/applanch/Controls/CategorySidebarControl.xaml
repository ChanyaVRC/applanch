<UserControl x:Class="applanch.Controls.CategorySidebarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:app="clr-namespace:applanch">
    <UserControl.Resources>
        <Style x:Key="CategorySidebarHoverZoneStyle"
               TargetType="Border">
            <Setter Property="Visibility"
                    Value="Visible"/>
            <Style.Triggers>
            <DataTrigger Binding="{Binding IsPinned, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Value="True">
                    <Setter Property="Visibility"
                            Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategorySidebarContainerStyle"
               TargetType="Border">
            <Setter Property="Padding"
                    Value="0"/>
            <Style.Triggers>
            <DataTrigger Binding="{Binding IsSidebarExpanded, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Value="True">
                    <Setter Property="Padding"
                            Value="12"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategorySidebarExpandedContentStyle"
               TargetType="Grid">
            <Setter Property="Visibility"
                    Value="Collapsed"/>
            <Setter Property="IsHitTestVisible"
                    Value="False"/>
            <Style.Triggers>
            <DataTrigger Binding="{Binding IsSidebarExpanded, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Value="True">
                    <Setter Property="Visibility"
                            Value="Visible"/>
                    <Setter Property="IsHitTestVisible"
                            Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border x:Name="CategorySidebarHoverZone"
                Style="{StaticResource CategorySidebarHoverZoneStyle}"
                Width="12"
                Margin="-12,0,0,0"
                HorizontalAlignment="Left"
                Background="Transparent"
                Panel.ZIndex="5"
                AllowDrop="True"
                MouseEnter="CategorySidebarHoverZone_MouseEnter"
                MouseLeave="CategorySidebarHoverZone_MouseLeave"
                DragOver="CategorySidebarHoverZone_DragOver"
                DragLeave="CategorySidebarHoverZone_DragLeave"/>

        <Border x:Name="CategorySidebarContainer"
                Style="{StaticResource CategorySidebarContainerStyle}"
                Width="{x:Static app:MainWindow.CategorySidebarExpandedWidth}"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.ItemBorder}"
                BorderThickness="1"
                CornerRadius="8"
                ClipToBounds="True"
                Panel.ZIndex="10"
                DragOver="CategorySidebarContainer_DragOver"
                DragLeave="CategorySidebarContainer_DragLeave"
                Drop="CategorySidebarContainer_Drop"
                MouseEnter="CategorySidebarContainer_MouseEnter"
                MouseLeave="CategorySidebarContainer_MouseLeave">
            <Grid>
                <Grid x:Name="CategorySidebarExpandedContent"
                      Style="{StaticResource CategorySidebarExpandedContentStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Source={x:Static app:LocalizedStrings.Instance}, Path=[SectionLabel_Category]}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.TextPrimary}"
                                   VerticalAlignment="Center"/>

                        <ToggleButton x:Name="CategorySidebarPinToggleButton"
                                      Grid.Column="1"
                                      Width="24"
                                      Height="24"
                                      Margin="8,0,0,0"
                                      Padding="0"
                                      Background="Transparent"
                                      BorderBrush="Transparent"
                                      BorderThickness="0"
                                      Cursor="Hand"
                                      Foreground="{DynamicResource Brush.TextPrimary}"
                                      FontSize="12"
                                      IsChecked="{Binding IsPinned, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                                      Click="CategorySidebarPinToggleButton_Click">
                            <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="ToolTip"
                                            Value="{Binding Source={x:Static app:LocalizedStrings.Instance}, Path=[ToolTip_UnpinCategorySidebar]}"/>
                                    <Setter Property="AutomationProperties.Name"
                                            Value="{Binding Source={x:Static app:LocalizedStrings.Instance}, Path=[ToolTip_UnpinCategorySidebar]}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="ToggleBorder"
                                                        Background="Transparent"
                                                        CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver"
                                                             Value="True">
                                                        <Setter TargetName="ToggleBorder"
                                                                Property="Background"
                                                                Value="{DynamicResource Brush.ItemBackground}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsChecked"
                                                             Value="False">
                                                        <Setter Property="Opacity"
                                                                Value="0.55"/>
                                                        <Setter Property="ToolTip"
                                                                Value="{Binding Source={x:Static app:LocalizedStrings.Instance}, Path=[ToolTip_PinCategorySidebar]}"/>
                                                        <Setter Property="AutomationProperties.Name"
                                                            Value="{Binding Source={x:Static app:LocalizedStrings.Instance}, Path=[ToolTip_PinCategorySidebar]}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ToggleButton.Style>
                            <Grid Width="14"
                                  Height="14">
                                <TextBlock Text="&#xE840;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                                <Border Width="14"
                                        Height="1.4"
                                        Background="{DynamicResource Brush.TextPrimary}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Opacity="0.9"
                                        RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <RotateTransform Angle="-35"/>
                                    </Border.RenderTransform>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                                             Value="False">
                                                    <Setter Property="Visibility"
                                                            Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </Grid>
                        </ToggleButton>
                    </Grid>

                    <ListBox Grid.Row="1"
                             Margin="0,10,0,0"
                             ItemsSource="{Binding FilterCategoryNames}"
                             SelectedItem="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="AllowDrop"
                                        Value="True"/>
                                <Setter Property="Padding"
                                        Value="0"/>
                                <Setter Property="Margin"
                                        Value="0,0,0,4"/>
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Stretch"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource Brush.TextSecondary}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="ItemBorder"
                                                    Padding="10,6"
                                                    CornerRadius="6"
                                                    Background="Transparent"
                                                    BorderThickness="1"
                                                    BorderBrush="Transparent">
                                                <ContentPresenter HorizontalAlignment="Stretch"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver"
                                                         Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource Brush.TextPrimary}"/>
                                                </Trigger>
                                                <Trigger Property="app:MainWindow.IsCategoryDropTarget"
                                                         Value="True">
                                                    <Setter TargetName="ItemBorder"
                                                            Property="Background"
                                                            Value="{DynamicResource Brush.ItemBackground}"/>
                                                    <Setter TargetName="ItemBorder"
                                                            Property="BorderBrush"
                                                            Value="{DynamicResource Brush.SurfaceBorder}"/>
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource Brush.TextPrimary}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected"
                                                         Value="True">
                                                    <Setter TargetName="ItemBorder"
                                                            Property="Background"
                                                            Value="{DynamicResource Brush.ItemBackground}"/>
                                                    <Setter TargetName="ItemBorder"
                                                            Property="BorderBrush"
                                                            Value="{DynamicResource Brush.SurfaceBorder}"/>
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource Brush.TextPrimary}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           FontSize="13"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Border x:Name="CategorySidebarCreateDropTarget"
                            Grid.Row="1"
                            Width="30"
                            Height="30"
                            Margin="0,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            AllowDrop="True"
                            Background="Transparent"
                            BorderBrush="{DynamicResource Brush.ItemBorder}"
                            BorderThickness="1"
                            CornerRadius="6"
                            DragOver="CategorySidebarCreateDropTarget_DragOver"
                            DragLeave="CategorySidebarCreateDropTarget_DragLeave"
                            Drop="CategorySidebarCreateDropTarget_Drop"
                            ToolTip="{Binding Source={x:Static app:LocalizedStrings.Instance}, Path=[ToolTip_CreateCategoryDropTarget]}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility"
                                        Value="Collapsed"/>
                                <Setter Property="Background"
                                        Value="Transparent"/>
                                <Setter Property="BorderBrush"
                                        Value="{DynamicResource Brush.ItemBorder}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCreateDropTargetVisible, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsCreateDropTargetActive, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 Value="True">
                                        <Setter Property="Background"
                                                Value="{DynamicResource Brush.ItemBackground}"/>
                                        <Setter Property="BorderBrush"
                                                Value="{DynamicResource Brush.SurfaceBorder}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="+"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.TextPrimary}"
                                   IsHitTestVisible="False"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
